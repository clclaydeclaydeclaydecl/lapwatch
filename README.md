# Lapwatch

A dead-simple TUI monitor for old laptops turned into servers. CPU temp, RAM, disk, battery, network.

## Installation

via terminal, do:
cat > ~/lapwatch.sh << 'EOF'

(lapwatch code here)

EOF

chmod +x ~/lapwatch.sh

## Usage

./lapwatch.sh

## Tested On

Ubuntu Server 22.04
